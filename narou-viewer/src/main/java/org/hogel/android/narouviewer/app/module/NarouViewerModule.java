package org.hogel.android.narouviewer.app.module;

import com.google.inject.Binder;
import com.google.inject.Module;

public class NarouViewerModule implements Module {
    @Override
    public void configure(final Binder binder) {
    }
}
